<%= form_with model: current_user.cart.cart_items.new do |f| %> 
  <%= f.hidden_field :food_id, value: @food.id %> 
  <%= f.label :quantity %> 
  <%= f.number_field :quantity, value: 1, min: 1 %> 
  <%= f.submit "Add to Cart" %> 
<% end %>
